import React, { useEffect, useRef, useState } from "react";
import {
  Home,
  ShieldCheck,
  Clock,
  LineChart,
  Star,
  Check,
  ChevronDown,
  Menu,
  X,
  Twitter,
  Github,
  Linkedin,
  Instagram,
  Mail,
  ArrowRight,
  Building2,
  Users,
  Phone,
} from "lucide-react";

const MortgageLandingPage = () => {
  // Navbar scroll background
  const [navSolid, setNavSolid] = useState(false);
  const [mobileOpen, setMobileOpen] = useState(false);
  const [billingPeriod, setBillingPeriod] = useState("monthly"); // 'monthly' | 'yearly'
  const [heroLoaded, setHeroLoaded] = useState(false);

  useEffect(() => {
    setHeroLoaded(true);
    const onScroll = () => {
      setNavSolid(window.scrollY > 50);
    };
    window.addEventListener("scroll", onScroll);
    return () => window.removeEventListener("scroll", onScroll);
  }, []);

  // Smooth scroll
  const handleScrollTo = (id) => {
    const el = document.getElementById(id);
    if (el) {
      el.scrollIntoView({ behavior: "smooth", block: "start" });
      setMobileOpen(false);
    }
  };

  // Intersection Observer hook
  const useInView = (options = { threshold: 0.2 }) => {
    const ref = useRef(null);
    const [visible, setVisible] = useState(false);

    useEffect(() => {
      const observer = new IntersectionObserver(([entry]) => {
        if (entry.isIntersecting) {
          setVisible(true);
          observer.unobserve(entry.target);
        }
      }, options);

      if (ref.current) {
        observer.observe(ref.current);
      }

      return () => observer.disconnect();
    }, [options]);

    return [ref, visible];
  };

  // Animated stat counter
  const StatCounter = ({ value, suffix = "", start }) => {
    const [display, setDisplay] = useState(0);

    useEffect(() => {
      if (!start) return;

      const duration = 2000;
      const frameRate = 60;
      const totalFrames = Math.round((duration / 1000) * frameRate);
      let currentFrame = 0;

      const interval = setInterval(() => {
        currentFrame += 1;
        const progress = Math.min(currentFrame / totalFrames, 1);
        const eased = 1 - Math.pow(1 - progress, 3); // easeOutCubic
        setDisplay(Math.round(value * eased));

        if (progress === 1) {
          clearInterval(interval);
        }
      }, 1000 / frameRate);

      return () => clearInterval(interval);
    }, [start, value]);

    return (
      <span>
        {display.toLocaleString("ru-RU")}
        {suffix}
      </span>
    );
  };

  // Sections in-view states
  const [featuresRef, featuresVisible] = useInView();
  const [statsRef, statsVisible] = useInView();
  const [howRef, howVisible] = useInView();
  const [testimonialsRef, testimonialsVisible] = useInView();
  const [pricingRef, pricingVisible] = useInView();
  const [faqRef, faqVisible] = useInView();
  const [ctaRef, ctaVisible] = useInView();

  // FAQ accordion
  const [openFaq, setOpenFaq] = useState(0);

  const pricing = [
    {
      name: "Базовый",
      priceMonthly: 0,
      priceYearly: 0,
      description: "Идеально для первого знакомства с ипотекой.",
      features: [
        "Первичная консультация по ипотеке",
        "Анализ кредитной истории",
        "Подбор до 3 программ",
        "Рекомендации по повышению одобрения",
      ],
      highlighted: false,
    },
    {
      name: "Про",
      tag: "Популярный",
      priceMonthly: 89,
      priceYearly: 790,
      description:
        "Полное сопровождение от заявки до получения ключей с максимальной выгодой.",
      features: [
        "Подбор из 50+ ипотечных программ",
        "Переговоры с банками за вас",
        "Оптимизация ставки и условий",
        "Сопровождение сделки юристом-партнёром",
        "Приоритетная поддержка 7/7",
      ],
      highlighted: true,
    },
    {
      name: "Энтерпрайз",
      priceMonthly: 199,
      priceYearly: 1790,
      description: "Решения для инвесторов и сложных кейсов по недвижимости.",
      features: [
        "Сложные схемы и инвестиционные сделки",
        "Мультиобъектные ипотечные конструкции",
        "Работа с доходом из разных стран",
        "Индивидуальная стратегия по портфелю",
      ],
      highlighted: false,
    },
  ];

  const faqItems = [
    {
      q: "Чем ипотечный брокер лучше прямого обращения в банк?",
      a: "Мы работаем сразу с десятками банков, знаем их внутренние критерии и можем подобрать программу с лучшей ставкой и условиями конкретно под вашу ситуацию, а не только из линейки одного банка.",
    },
    {
      q: "Сколько стоят ваши услуги?",
      a: "Первичная консультация — бесплатно. Полное сопровождение обычно окупается за счёт более низкой ставки и экономии времени. Точную стоимость мы озвучиваем после оценки вашей ситуации.",
    },
    {
      q: "Можно ли получить ипотеку с неофициальным доходом?",
      a: "Во многих банках есть продукты, учитывающие косвенные подтверждения дохода. Мы заранее знаем, в какие банки есть смысл обращаться, чтобы не портить кредитную историю отказами.",
    },
    {
      q: "Сколько времени занимает одобрение ипотеки?",
      a: "В среднем от 1 до 5 рабочих дней. Для срочных сделок мы готовим пакет документов заранее и подаём заявки параллельно в несколько банков.",
    },
    {
      q: "Вы работаете только с новостройками или и с вторичным рынком?",
      a: "Мы сопровождаем сделки как с новостройками, так и с готовым жильём, а также с рефинансированием текущей ипотеки.",
    },
    {
      q: "Можно ли общаться дистанционно?",
      a: "Да, полный процесс — от консультации до одобрения — можем провести онлайн, без необходимости личных визитов до момента сделки.",
    },
  ];

  const heroButtons = [
    {
      label: "Рассчитать ипотеку",
      primary: true,
      onClick: () => handleScrollTo("pricing"),
    },
    {
      label: "Бесплатная консультация",
      primary: false,
      onClick: () => handleScrollTo("cta"),
    },
  ];

  const navLinks = [
    { id: "features", label: "Преимущества" },
    { id: "how", label: "Как это работает" },
    { id: "stats", label: "Цифры" },
    { id: "testimonials", label: "Отзывы" },
    { id: "pricing", label: "Тарифы" },
    { id: "faq", label: "FAQ" },
  ];

  return (
    <>
      {/* Inline styles with animations & theme */}
      <style>{`
        :root {
          --bg-primary: #080810;
          --accent-from: #10b981; /* emerald */
          --accent-to: #22d3ee;   /* cyan */
          --card-bg: rgba(15, 23, 42, 0.7);
          --border-color: rgba(148, 163, 184, 0.18);
        }

        html {
          scroll-behavior: smooth;
        }

        .gradient-text {
          background: linear-gradient(135deg, var(--accent-from), var(--accent-to));
          -webkit-background-clip: text;
          background-clip: text;
          color: transparent;
        }

        .btn-primary {
          background: linear-gradient(135deg, var(--accent-from), var(--accent-to));
          box-shadow: 0 0 30px rgba(16, 185, 129, 0.45);
          transition: transform 0.2s ease, box-shadow 0.2s ease, filter 0.2s ease;
        }
        .btn-primary:hover {
          transform: scale(1.05) translateY(-1px);
          box-shadow: 0 0 40px rgba(34, 211, 238, 0.55);
          filter: brightness(1.05);
        }

        .btn-ghost {
          background: radial-gradient(circle at top left, rgba(148, 163, 184, 0.25), transparent);
          border: 1px solid rgba(148, 163, 184, 0.4);
          transition: transform 0.2s ease, box-shadow 0.2s ease, background 0.2s ease, border-color 0.2s ease;
        }
        .btn-ghost:hover {
          transform: scale(1.05) translateY(-1px);
          box-shadow: 0 0 30px rgba(148, 163, 184, 0.4);
          border-color: rgba(226, 232, 240, 0.9);
          background: radial-gradient(circle at top left, rgba(226, 232, 240, 0.18), transparent);
        }

        .card-glass {
          background: radial-gradient(circle at top left, rgba(15, 118, 110, 0.35), transparent),
                      radial-gradient(circle at bottom right, rgba(56, 189, 248, 0.25), transparent),
                      var(--card-bg);
          border: 1px solid var(--border-color);
          backdrop-filter: blur(16px);
          -webkit-backdrop-filter: blur(16px);
          box-shadow: 0 18px 40px rgba(15, 23, 42, 0.85);
          transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease, background 0.2s ease;
        }

        .card-glass-hover:hover {
          transform: translateY(-4px) scale(1.03);
          border-color: rgba(56, 189, 248, 0.8);
          box-shadow: 0 20px 60px rgba(8, 47, 73, 0.9);
        }

        .section-hidden {
          opacity: 0;
          transform: translateY(32px);
          transition: opacity 0.6s ease-out, transform 0.6s ease-out;
        }
        .section-visible {
          opacity: 1;
          transform: translateY(0);
        }

        @keyframes fadeInUp {
          from {
            opacity: 0;
            transform: translateY(30px);
          }
          to {
            opacity: 1;
            transform: translateY(0);
          }
        }

        .hero-animate {
          animation: fadeInUp 0.8s ease-out forwards;
        }

        .blob {
          position: absolute;
          border-radius: 999px;
          filter: blur(28px);
          opacity: 0.7;
          mix-blend-mode: screen;
          animation: blobFloat 18s ease-in-out infinite alternate;
        }

        @keyframes blobFloat {
          0% {
            transform: translate3d(0, 0, 0) scale(1);
          }
          50% {
            transform: translate3d(40px, -40px, 0) scale(1.15);
          }
          100% {
            transform: translate3d(-30px, 30px, 0) scale(1.05);
          }
        }

        .nav-blur {
          backdrop-filter: blur(18px);
          -webkit-backdrop-filter: blur(18px);
          background: radial-gradient(circle at top left, rgba(16, 185, 129, 0.08), transparent),
                      rgba(15, 23, 42, 0.88);
          box-shadow: 0 18px 40px rgba(15, 23, 42, 0.9);
          border-bottom: 1px solid rgba(148, 163, 184, 0.22);
        }

        .badge-gradient {
          background: linear-gradient(135deg, var(--accent-from), var(--accent-to));
          box-shadow: 0 0 20px rgba(16, 185, 129, 0.5);
        }

        .faq-body {
          transition: max-height 0.3s ease, opacity 0.3s ease, transform 0.3s ease;
        }

        .chevron-rotate {
          transition: transform 0.25s ease;
        }

        .stat-separator {
          background: linear-gradient(90deg, transparent, var(--accent-from), var(--accent-to), transparent);
        }

        .cta-gradient {
          background: radial-gradient(circle at top left, rgba(34, 211, 238, 0.35), transparent),
                      radial-gradient(circle at bottom right, rgba(16, 185, 129, 0.4), transparent),
                      linear-gradient(135deg, #0f172a, #020617);
          box-shadow: 0 24px 80px rgba(15, 23, 42, 0.95);
        }

        .input-glass {
          background: rgba(15, 23, 42, 0.8);
          border: 1px solid rgba(148, 163, 184, 0.45);
          box-shadow: 0 12px 40px rgba(15, 23, 42, 0.9);
        }

        .social-icon {
          transition: transform 0.2s ease, color 0.2s ease, box-shadow 0.2s ease, background 0.2s ease;
        }

        .social-icon:hover {
          transform: translateY(-2px) scale(1.08);
          color: #e5e7eb;
          background: radial-gradient(circle at top left, rgba(148, 163, 184, 0.25), transparent);
          box-shadow: 0 12px 35px rgba(15, 23, 42, 0.95);
        }

        @media (max-width: 767px) {
          .hero-title {
            font-size: 2.25rem;
            line-height: 2.6rem;
          }
          .hero-subtitle {
            font-size: 0.95rem;
          }
        }

        @media (min-width: 768px) {
          .hero-title {
            font-size: 3rem;
            line-height: 3.2rem;
          }
        }

        @media (min-width: 1024px) {
          .hero-title {
            font-size: 3.75rem;
            line-height: 4.1rem;
          }
        }
      `}</style>

      <div className="min-h-screen bg-[radial-gradient(circle_at_top,_#1e293b_0,_transparent_55%),radial-gradient(circle_at_bottom,_#020617_0,_#020617_65%)] text-slate-100">
        {/* Floating blobs */}
        <div
          className="blob -top-24 -left-16 w-72 h-72 bg-emerald-500/40"
          style={{ zIndex: 0 }}
        />
        <div
          className="blob top-1/3 -right-24 w-80 h-80 bg-cyan-500/35"
          style={{ animationDelay: "5s", zIndex: 0 }}
        />
        <div
          className="blob bottom-0 left-1/4 w-64 h-64 bg-indigo-500/25"
          style={{ animationDelay: "10s", zIndex: 0 }}
        />

        {/* NAVBAR */}
        <header
          className={`fixed top-0 inset-x-0 z-30 ${
            navSolid ? "nav-blur" : "bg-transparent"
          } transition-colors duration-300`}
        >
          <div className="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8 py-4 flex items-center justify-between">
            {/* Logo */}
            <button
              onClick={() => handleScrollTo("hero")}
              className="flex items-center gap-2 text-slate-100"
            >
              <div className="flex h-9 w-9 items-center justify-center rounded-2xl bg-slate-900/80 border border-emerald-400/60 shadow-lg shadow-emerald-500/40">
                <Building2 className="h-5 w-5 text-emerald-400" />
              </div>
              <div className="flex flex-col items-start leading-tight">
                <span className="text-sm font-semibold tracking-tight">
                  Emerald
                  <span className="text-emerald-400">Mortgage</span>
                </span>
                <span className="text-[11px] text-slate-400">
                  Ипотечный брокер нового поколения
                </span>
              </div>
            </button>

            {/* Desktop nav */}
            <nav className="hidden md:flex items-center gap-8 text-sm">
              {navLinks.map((link) => (
                <button
                  key={link.id}
                  onClick={() => handleScrollTo(link.id)}
                  className="text-slate-300 hover:text-slate-50 transition-colors"
                >
                  {link.label}
                </button>
              ))}
            </nav>

            {/* Desktop CTA */}
            <div className="hidden md:flex items-center gap-3">
              <button
                onClick={() => handleScrollTo("cta")}
                className="btn-ghost rounded-full px-4 py-2 text-xs font-medium flex items-center gap-2"
              >
                <Phone className="h-4 w-4 text-emerald-400" />
                <span>Связаться</span>
              </button>
              <button
                onClick={() => handleScrollTo("pricing")}
                className="btn-primary rounded-full px-5 py-2.5 text-xs font-semibold flex items-center gap-2"
              >
                <span>Рассчитать ставку</span>
                <ArrowRight className="h-4 w-4" />
              </button>
            </div>

            {/* Mobile menu button */}
            <button
              className="md:hidden inline-flex items-center justify-center rounded-full border border-slate-700/70 bg-slate-900/70 p-2"
              onClick={() => setMobileOpen((v) => !v)}
            >
              {mobileOpen ? (
                <X className="h-5 w-5 text-slate-100" />
              ) : (
                <Menu className="h-5 w-5 text-slate-100" />
              )}
            </button>
          </div>

          {/* Mobile drawer */}
          {mobileOpen && (
            <div className="md:hidden border-t border-slate-800/70 bg-slate-950/95 backdrop-blur-xl">
              <div className="mx-auto max-w-6xl px-4 py-4 space-y-3">
                {navLinks.map((link) => (
                  <button
                    key={link.id}
                    onClick={() => handleScrollTo(link.id)}
                    className="block w-full text-left text-sm text-slate-200 py-1"
                  >
                    {link.label}
                  </button>
                ))}
                <div className="pt-3 flex flex-col gap-2">
                  <button
                    onClick={() => handleScrollTo("cta")}
                    className="btn-ghost rounded-full px-4 py-2 text-xs font-medium flex items-center justify-center gap-2"
                  >
                    <Phone className="h-4 w-4 text-emerald-400" />
                    <span>Связаться</span>
                  </button>
                  <button
                    onClick={() => handleScrollTo("pricing")}
                    className="btn-primary rounded-full px-5 py-2.5 text-xs font-semibold flex items-center justify-center gap-2"
                  >
                    <span>Рассчитать ставку</span>
                    <ArrowRight className="h-4 w-4" />
                  </button>
                </div>
              </div>
            </div>
          )}
        </header>

        {/* MAIN CONTENT */}
        <main className="relative z-10 pt-24 pb-20">
          {/* HERO */}
          <section
            id="hero"
            className="relative mx-auto max-w-6xl px-4 sm:px-6 lg:px-8 pt-10 pb-16 lg:pt-16 lg:pb-24"
          >
            {/* grid decoration */}
            <div className="pointer-events-none absolute inset-0 -z-10 opacity-30">
              <div className="h-full w-full bg-[linear-gradient(to_right,_rgba(148,163,184,0.12)_1px,_transparent_1px),linear-gradient(to_bottom,_rgba(148,163,184,0.12)_1px,_transparent_1px)] bg-[size:42px_42px]" />
            </div>

            <div className="lg:grid lg:grid-cols-12 lg:gap-10 items-center">
              <div
                className={`lg:col-span-7 space-y-8 ${
                  heroLoaded ? "hero-animate" : "opacity-0 translate-y-8"
                }`}
              >
                <div className="inline-flex items-center gap-2 rounded-full bg-slate-900/80 border border-emerald-500/40 px-3 py-1 text-[11px] text-emerald-300 shadow-lg shadow-emerald-500/30">
                  <SparklesSmall />
                  <span>Ипотека под ваш сценарий, а не под шаблон банка</span>
                </div>

                <h1 className="hero-title font-semibold tracking-tight text-slate-50">
                  <span className="block">Премиальный ипотечный брокер</span>
                  <span className="block gradient-text">
                    который экономит вам годы и миллионы рублей
                  </span>
                </h1>

                <p className="hero-subtitle max-w-xl text-sm sm:text-base text-slate-300">
                  Мы подбираем и согласуем ипотеку под вашу ситуацию, а не под
                  стандарты банка: сложный доход, бизнес, инвестиции,
                  рефинансирование — всё берём на себя и ведём до ключей.
                </p>

                <div className="flex flex-wrap items-center gap-4">
                  {heroButtons.map((btn) => (
                    <button
                      key={btn.label}
                      onClick={btn.onClick}
                      className={`rounded-full px-6 py-3 text-sm font-semibold flex items-center gap-2 min-h-[44px] ${
                        btn.primary ? "btn-primary" : "btn-ghost"
                      }`}
                    >
                      {btn.primary ? (
                        <>
                          <Home className="h-4 w-4" />
                          {btn.label}
                        </>
                      ) : (
                        <>
                          <Users className="h-4 w-4 text-emerald-400" />
                          {btn.label}
                        </>
                      )}
                    </button>
                  ))}
                </div>

                <div className="flex flex-wrap gap-6 text-xs text-slate-400 pt-2">
                  <div className="flex items-center gap-2">
                    <ShieldCheck className="h-4 w-4 text-emerald-400" />
                    <span>Лицензированные партнёры ведущих банков</span>
                  </div>
                  <div className="flex items-center gap-2">
                    <Clock className="h-4 w-4 text-cyan-400" />
                    <span>Одобрение от 1 рабочего дня</span>
                  </div>
                  <div className="flex items-center gap-2">
                    <LineChart className="h-4 w-4 text-emerald-300" />
                    <span>Снижаем эффективную ставку до 2,3% годовых*</span>
                  </div>
                </div>
              </div>

              {/* Hero visual */}
              <div
                className={`mt-10 lg:mt-0 lg:col-span-5 ${
                  heroLoaded ? "hero-animate" : "opacity-0 translate-y-8"
                } lg:translate-y-4`}
                style={{ animationDelay: "0.1s" }}
              >
                <div className="relative">
                  <div className="absolute -inset-0.5 bg-gradient-to-br from-emerald-500/70 via-cyan-400/60 to-sky-500/50 opacity-50 blur-2xl" />
                  <div className="relative card-glass rounded-3xl p-5 sm:p-6 lg:p-7">
                    <div className="flex items-center justify-between mb-5">
                      <div>
                        <p className="text-[11px] uppercase tracking-[0.16em] text-slate-400">
                          Ваш ипотечный профиль
                        </p>
                        <p className="mt-1 text-sm font-medium text-slate-100">
                          Вероятность одобрения:{" "}
                          <span className="text-emerald-400">92%</span>
                        </p>
                      </div>
                      <div className="inline-flex items-center gap-1 rounded-full bg-emerald-500/10 px-3 py-1 border border-emerald-400/40">
                        <SparklesSmall className="h-3 w-3 text-emerald-300" />
                        <span className="text-[11px] text-emerald-200">
                          AI-анализ
                        </span>
                      </div>
                    </div>

                    <div className="space-y-5">
                      <div className="grid grid-cols-3 gap-3 text-xs">
                        <HeroMetric
                          label="Ставка от"
                          value="2,3%"
                          accent
                          icon={<LineChart className="h-4 w-4 text-emerald-300" />}
                        />
                        <HeroMetric
                          label="Сумма займа"
                          value="18,5 млн ₽"
                        />
                        <HeroMetric
                          label="Срок"
                          value="25 лет"
                        />
                      </div>

                      <div className="h-[1px] bg-gradient-to-r from-transparent via-emerald-500/70 to-transparent opacity-60" />

                      <div className="space-y-3">
                        <p className="text-xs text-slate-300">
                          Топ-3 программы под ваш сценарий:
                        </p>
                        <div className="space-y-2">
                          <ProgramRow
                            bank="Альфа-Банк"
                            rate="2,3%"
                            note="IT-ипотека + субсидия застройщика"
                          />
                          <ProgramRow
                            bank="ВТБ"
                            rate="3,1%"
                            note="Новостройка, господдержка"
                          />
                          <ProgramRow
                            bank="Сбер"
                            rate="3,4%"
                            note="Семейная ипотека"
                          />
                        </div>
                      </div>

                      <div className="mt-3 flex items-center justify-between gap-3 rounded-2xl bg-slate-900/70 border border-slate-700/70 px-3 py-2.5">
                        <div className="flex items-center gap-2">
                          <div className="flex h-7 w-7 items-center justify-center rounded-full bg-emerald-500/15 border border-emerald-400/60">
                            <Users className="h-3.5 w-3.5 text-emerald-300" />
                          </div>
                          <div className="text-[11px] leading-tight">
                            <p className="text-slate-200">
                              Брокер подключится к вашему кейсу
                            </p>
                            <p className="text-slate-400">
                              В среднем экономим клиентам{" "}
                              <span className="text-emerald-300 font-medium">
                                от 350 000 ₽
                              </span>
                            </p>
                          </div>
                        </div>
                        <button
                          onClick={() => handleScrollTo("cta")}
                          className="hidden sm:inline-flex items-center gap-1 rounded-full border border-emerald-400/60 bg-emerald-500/10 px-3 py-1.5 text-[11px] font-medium text-emerald-200 hover:bg-emerald-500/20 transition-colors"
                        >
                          <span>Получить расчёт</span>
                          <ArrowRight className="h-3 w-3" />
                        </button>
                      </div>

                      <p className="mt-2 text-[10px] text-slate-500">
                        * Пример эффективной ставки с учётом субсидий, налогового
                        вычета и рефинансирования. Не является публичной офертой.
                      </p>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </section>

          {/* FEATURES */}
          <section
            id="features"
            ref={featuresRef}
            className="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8 py-10 sm:py-14 lg:py-16"
          >
            <div
              className={`section-hidden ${
                featuresVisible ? "section-visible" : ""
              }`}
            >
              <div className="flex items-center justify-between gap-4 mb-8">
                <div>
                  <h2 className="text-2xl sm:text-3xl font-semibold tracking-tight text-slate-50">
                    Почему с ипотекой проще с{" "}
                    <span className="gradient-text">EmeraldMortgage</span>
                  </h2>
                  <p className="mt-2 text-sm text-slate-300 max-w-xl">
                    Мы совмещаем опыт живых брокеров и точность алгоритмов, чтобы
                    превратить хаос ипотечных программ в понятный и выгодный
                    маршрут к вашей квартире.
                  </p>
                </div>
                <div className="hidden sm:flex text-xs text-slate-400 items-center gap-2">
                  <span className="inline-flex h-1.5 w-1.5 rounded-full bg-emerald-400 animate-pulse" />
                  <span>Онлайн-сопровождение по всей России</span>
                </div>
              </div>

              <div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">
                {[
                  {
                    icon: <ShieldCheck className="h-5 w-5 text-emerald-300" />,
                    title: "Сложные кейсы — наша рутина",
                    desc: "Бизнес, несколько источников дохода, неидеальная кредитная история, инвестиции — подключаем опыт старших брокеров и юристов.",
                  },
                  {
                    icon: <LineChart className="h-5 w-5 text-cyan-300" />,
                    title: "Максимум выгоды, минимум переплат",
                    desc: "Сравниваем десятки программ, считаем полную стоимость владения и подбираем схему, где вы платите меньше всего.",
                  },
                  {
                    icon: <Clock className="h-5 w-5 text-emerald-300" />,
                    title: "Одобрение в сжатые сроки",
                    desc: "Готовим идеальный пакет документов под конкретные банки, чтобы не терять время на отказы и доработки.",
                  },
                  {
                    icon: <Home className="h-5 w-5 text-sky-300" />,
                    title: "Работаем с застройщиками и рынком",
                    desc: "Подбираем выгодные схемы и субсидии от застройщиков, проверяем объекта и защищаем от скрытых рисков.",
                  },
                  {
                    icon: <Users className="h-5 w-5 text-emerald-300" />,
                    title: "Команда, а не один менеджер",
                    desc: "С вами одновременно работают брокер, аналитик и юрист-партнёр — каждый отвечает за свою часть сделки.",
                  },
                  {
                    icon: <Phone className="h-5 w-5 text-cyan-300" />,
                    title: "Поддержка 7 дней в неделю",
                    desc: "Мы на связи, когда банки закрыты: объясним, пересчитаем, подскажем, как правильно торговаться по цене.",
                  },
                ].map((feature, idx) => (
                  <div
                    key={feature.title}
                    className={`card-glass card-glass-hover rounded-2xl p-4 sm:p-5 flex flex-col gap-3 ${
                      featuresVisible
                        ? `delay-[${idx * 50}ms]`
                        : "opacity-0 translate-y-4"
                    }`}
                    style={{
                      transitionDelay: featuresVisible ? `${idx * 70}ms` : "0ms",
                    }}
                  >
                    <div className="flex items-center justify-between">
                      <div className="flex h-9 w-9 items-center justify-center rounded-xl bg-slate-900/80 border border-slate-600/60">
                        {feature.icon}
                      </div>
                      <span className="text-[11px] text-slate-400">
                        {idx + 1 < 10 ? `0${idx + 1}` : idx + 1}
                      </span>
                    </div>
                    <h3 className="text-sm font-semibold text-slate-50">
                      {feature.title}
                    </h3>
                    <p className="text-xs text-slate-300">{feature.desc}</p>
                  </div>
                ))}
              </div>
            </div>
          </section>

          {/* STATS */}
          <section
            id="stats"
            ref={statsRef}
            className="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8 py-8 sm:py-12"
          >
            <div
              className={`section-hidden ${
                statsVisible ? "section-visible" : ""
              }`}
            >
              <div className="stat-separator h-px mb-8 opacity-70" />
              <div className="grid gap-6 sm:grid-cols-4">
                <div className="sm:col-span-2 space-y-3">
                  <p className="text-xs uppercase tracking-[0.2em] text-emerald-300">
                    Цифры, за которыми стоят истории
                  </p>
                  <h2 className="text-2xl sm:text-3xl font-semibold tracking-tight text-slate-50">
                    Результаты, которые мы даём клиентам
                  </h2>
                  <p className="text-sm text-slate-300 max-w-md">
                    Каждую сделку мы считаем как собственную: что будет, если вы
                    продадите, рефинансируете или будете сдавать жильё через 5–10
                    лет.
                  </p>
                </div>

                <div className="flex flex-col gap-5 sm:col-span-2">
                  <StatBlock
                    label="Клиентов одобрено за прошлый год"
                    value={870}
                    suffix="+"
                    start={statsVisible}
                  />
                  <StatBlock
                    label="Средняя экономия по переплате за весь срок"
                    value={18}
                    suffix="%"
                    start={statsVisible}
                  />
                  <StatBlock
                    label="Сделок с одобрением с первого захода"
                    value={94}
                    suffix="%"
                    start={statsVisible}
                  />
                </div>
              </div>
            </div>
          </section>

          {/* HOW IT WORKS */}
          <section
            id="how"
            ref={howRef}
            className="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8 py-10 sm:py-14 lg:py-16"
          >
            <div
              className={`section-hidden ${howVisible ? "section-visible" : ""}`}
            >
              <div className="flex flex-col sm:flex-row sm:items-end justify-between gap-5 mb-9">
                <div>
                  <p className="text-xs uppercase tracking-[0.2em] text-emerald-300">
                    Как это работает
                  </p>
                  <h2 className="mt-2 text-2xl sm:text-3xl font-semibold tracking-tight text-slate-50">
                    Три шага до ключей от вашей квартиры
                  </h2>
                </div>
                <p className="text-sm text-slate-300 max-w-md">
                  Берём на себя всё: от стратегической модели до согласования
                  в банке и сопровождения сделки у застройщика или на вторичке.
                </p>
              </div>

              <div className="flex flex-col lg:flex-row items-stretch gap-6">
                {[
                  {
                    title: "Диагностика и стратегия",
                    desc: "Разбираем вашу ситуацию, цели и горизонты. Строим несколько сценариев и считаем реальную стоимость владения.",
                    icon: <Users className="h-5 w-5 text-emerald-300" />,
                  },
                  {
                    title: "Подбор и одобрение",
                    desc: "Подаём заявки в релевантные банки, торгуемся по ставкам, подбираем объект и безопасную юридическую схему.",
                    icon: <ShieldCheck className="h-5 w-5 text-cyan-300" />,
                  },
                  {
                    title: "Сделка и сопровождение",
                    desc: "Контролируем банк, застройщика, риелтора и юристов. Собираем подписи, проверяем документы, остаёмся с вами после сделки.",
                    icon: <Home className="h-5 w-5 text-sky-300" />,
                  },
                ].map((step, idx) => (
                  <div
                    key={step.title}
                    className="relative flex-1"
                  >
                    {/* connector line */}
                    {idx < 2 && (
                      <div className="hidden lg:block absolute top-8 -right-3 w-6 h-px bg-gradient-to-r from-emerald-500/70 via-cyan-400/70 to-transparent opacity-75" />
                    )}
                    <div className="card-glass card-glass-hover rounded-2xl p-5 sm:p-6 flex flex-col gap-4 h-full">
                      <div className="flex items-center justify-between gap-3">
                        <div className="flex items-center gap-3">
                          <div className="badge-gradient flex h-7 w-7 items-center justify-center rounded-full text-[12px] font-semibold text-slate-950 border border-emerald-300/80">
                            {idx + 1}
                          </div>
                          <div className="flex h-8 w-8 items-center justify-center rounded-xl bg-slate-900/80 border border-slate-600/70">
                            {step.icon}
                          </div>
                        </div>
                        <span className="text-[11px] text-slate-400">
                          ~{idx === 0 ? "30–40 мин" : idx === 1 ? "1–5 дней" : "1 день"}
                        </span>
                      </div>
                      <div>
                        <h3 className="text-sm font-semibold text-slate-50 mb-1.5">
                          {step.title}
                        </h3>
                        <p className="text-xs text-slate-300">{step.desc}</p>
                      </div>
                    </div>
                  </div>
                ))}
              </div>
            </div>
          </section>

          {/* TESTIMONIALS */}
          <section
            id="testimonials"
            ref={testimonialsRef}
            className="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8 py-10 sm:py-14 lg:py-16"
          >
            <div
              className={`section-hidden ${
                testimonialsVisible ? "section-visible" : ""
              }`}
            >
              <div className="flex flex-col sm:flex-row sm:items-end justify-between gap-5 mb-8">
                <div>
                  <p className="text-xs uppercase tracking-[0.2em] text-emerald-300">
                    Отзывы клиентов
                  </p>
                  <h2 className="mt-2 text-2xl sm:text-3xl font-semibold tracking-tight text-slate-50">
                    Истории, которые вдохновляют не хуже ставок
                  </h2>
                </div>
                <p className="text-sm text-slate-300 max-w-md">
                  Мы не просто «одобряем ипотеку» — мы строим долгосрочные
                  отношения и финансовые стратегии.
                </p>
              </div>

              <div className="flex gap-4 overflow-x-auto pb-4 sm:grid sm:grid-cols-3 sm:gap-5 sm:overflow-visible">
                {[
                  {
                    name: "Анна К.",
                    role: "Product Manager в IT, Москва",
                    initials: "АК",
                    text: "Думала, что с моим графиком и опционами в доходе банк просто не поймёт, как меня считать. Брокеры аккуратно разложили всё по банкам, где такие кейсы — норма. В итоге ставка ниже, чем предлагали напрямую.",
                  },
                  {
                    name: "Игорь и Мария Л.",
                    role: "Предприниматели, Санкт-Петербург",
                    initials: "ИМ",
                    text: "Было три объекта, один в залоге, плюс планировали рефинансирование. В банках только путали ещё больше. Здесь нарисовали понятную схему на доске, мы впервые увидели картину целиком.",
                  },
                  {
                    name: "Олег Н.",
                    role: "Инвестор в недвижимость",
                    initials: "ОН",
                    text: "Искал не просто ипотеку, а рабочий инструмент под портфель из сдаваемых квартир. Помогли собрать конструкцию, где недвижимость почти целиком окупает себя арендой.",
                  },
                ].map((t, idx) => (
                  <article
                    key={t.name}
                    className="min-w-[260px] sm:min-w-0 card-glass card-glass-hover rounded-2xl p-4 sm:p-5 flex flex-col gap-4"
                    style={{
                      transitionDelay: testimonialsVisible
                        ? `${idx * 80}ms`
                        : "0ms",
                    }}
                  >
                    <div className="flex items-center justify-between gap-3">
                      <div className="flex items-center gap-3">
                        <div className="flex h-9 w-9 items-center justify-center rounded-full bg-gradient-to-br from-emerald-500 to-cyan-500 text-[11px] font-semibold text-slate-900 shadow-lg shadow-emerald-500/40">
                          {t.initials}
                        </div>
                        <div className="text-xs">
                          <p className="font-semibold text-slate-100">
                            {t.name}
                          </p>
                          <p className="text-[11px] text-slate-400">
                            {t.role}
                          </p>
                        </div>
                      </div>
                      <div className="flex items-center gap-0.5 text-amber-300">
                        {Array.from({ length: 5 }).map((_, i) => (
                          <Star
                            key={i}
                            className="h-3.5 w-3.5 fill-amber-300/80"
                          />
                        ))}
                      </div>
                    </div>
                    <p className="text-xs text-slate-200 leading-relaxed">
                      “{t.text}”
                    </p>
                  </article>
                ))}
              </div>
            </div>
          </section>

          {/* PRICING */}
          <section
            id="pricing"
            ref={pricingRef}
            className="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8 py-10 sm:py-14 lg:py-16"
          >
            <div
              className={`section-hidden ${
                pricingVisible ? "section-visible" : ""
              }`}
            >
              <div className="flex flex-col sm:flex-row sm:items-end justify-between gap-5 mb-8">
                <div>
                  <p className="text-xs uppercase tracking-[0.2em] text-emerald-300">
                    Тарифы
                  </p>
                  <h2 className="mt-2 text-2xl sm:text-3xl font-semibold tracking-tight text-slate-50">
                    Прозрачные форматы работы без сюрпризов
                  </h2>
                </div>

                {/* Billing toggle */}
                <div className="flex flex-col sm:items-end gap-2 text-xs">
                  <div className="inline-flex items-center gap-2 rounded-full bg-slate-900/80 border border-slate-700/70 px-1.5 py-1">
                    <button
                      onClick={() => setBillingPeriod("monthly")}
                      className={`px-3 py-1 rounded-full font-medium ${
                        billingPeriod === "monthly"
                          ? "bg-slate-100 text-slate-900"
                          : "text-slate-300"
                      }`}
                    >
                      Помесячно
                    </button>
                    <button
                      onClick={() => setBillingPeriod("yearly")}
                      className={`px-3 py-1 rounded-full font-medium flex items-center gap-1 ${
                        billingPeriod === "yearly"
                          ? "bg-emerald-500 text-slate-950"
                          : "text-slate-300"
                      }`}
                    >
                      <span>За год</span>
                      <span className="text-[10px] rounded-full bg-emerald-900/70 px-1.5 py-0.5 text-emerald-200 border border-emerald-400/60">
                        -25%
                      </span>
                    </button>
                  </div>
                  <p className="text-[11px] text-slate-400">
                    Оплата за результат, а не за попытки. Обсуждается индивидуально.
                  </p>
                </div>
              </div>

              <div className="grid gap-5 lg:grid-cols-3">
                {pricing.map((tier, idx) => {
                  const isYear = billingPeriod === "yearly";
                  const price = isYear ? tier.priceYearly : tier.priceMonthly;
                  const periodLabel = isYear ? "/ год" : "/ мес";

                  return (
                    <div
                      key={tier.name}
                      className={`relative card-glass card-glass-hover rounded-2xl p-5 sm:p-6 flex flex-col gap-4 ${
                        tier.highlighted
                          ? "border-emerald-400/80 shadow-emerald-500/50 scale-[1.02]"
                          : ""
                      }`}
                      style={{
                        transform:
                          tier.highlighted && pricingVisible
                            ? "translateY(-4px) scale(1.02)"
                            : undefined,
                      }}
                    >
                      {tier.tag && (
                        <div className="absolute -top-3 right-5">
                          <div className="badge-gradient rounded-full px-3 py-1 text-[11px] font-semibold text-slate-950">
                            {tier.tag}
                          </div>
                        </div>
                      )}
                      <div className="flex items-center justify-between">
                        <div>
                          <h3 className="text-sm font-semibold text-slate-50">
                            {tier.name}
                          </h3>
                          <p className="text-xs text-slate-400 mt-1">
                            {tier.description}
                          </p>
                        </div>
                      </div>

                      <div className="flex items-baseline gap-1 mt-1">
                        <span className="text-2xl sm:text-3xl font-semibold text-slate-50">
                          {price === 0 ? "Индивидуально" : `${price}K`}
                        </span>
                        {price !== 0 && (
                          <span className="text-xs text-slate-400">
                            {periodLabel}
                          </span>
                        )}
                      </div>

                      <ul className="mt-3 space-y-2 text-xs text-slate-200">
                        {tier.features.map((f) => (
                          <li key={f} className="flex items-start gap-2">
                            <Check className="mt-[2px] h-3.5 w-3.5 text-emerald-300" />
                            <span>{f}</span>
                          </li>
                        ))}
                      </ul>

                      <div className="mt-4">
                        <button
                          onClick={() => handleScrollTo("cta")}
                          className={`w-full rounded-full px-4 py-2.5 text-xs font-semibold flex items-center justify-center gap-2 min-h-[44px] ${
                            tier.highlighted ? "btn-primary" : "btn-ghost"
                          }`}
                        >
                          <span>
                            {tier.highlighted
                              ? "Хочу полное сопровождение"
                              : "Обсудить детали"}
                          </span>
                          <ArrowRight className="h-4 w-4" />
                        </button>
                      </div>
                    </div>
                  );
                })}
              </div>
            </div>
          </section>

          {/* FAQ */}
          <section
            id="faq"
            ref={faqRef}
            className="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8 py-10 sm:py-14 lg:py-16"
          >
            <div
              className={`section-hidden ${faqVisible ? "section-visible" : ""}`}
            >
              <div className="flex flex-col sm:flex-row sm:items-start justify-between gap-6 mb-8">
                <div className="sm:w-1/3">
                  <p className="text-xs uppercase tracking-[0.2em] text-emerald-300">
                    FAQ
                  </p>
                  <h2 className="mt-2 text-2xl sm:text-3xl font-semibold tracking-tight text-slate-50">
                    Отвечаем на вопросы, которые обычно остаются «на потом»
                  </h2>
                  <p className="mt-3 text-sm text-slate-300">
                    Если не нашли ответ — просто напишите нам. Мы честно скажем,
                    сможем ли быть полезны в вашей ситуации.
                  </p>
                </div>

                <div className="sm:w-2/3 grid gap-3 sm:grid-cols-2">
                  {faqItems.map((item, idx) => {
                    const isOpen = openFaq === idx;
                    return (
                      <div
                        key={item.q}
                        className="card-glass rounded-2xl p-3.5 sm:p-4"
                      >
                        <button
                          onClick={() =>
                            setOpenFaq((prev) => (prev === idx ? -1 : idx))
                          }
                          className="w-full flex items-start justify-between gap-3 text-left"
                        >
                          <span className="text-[13px] font-medium text-slate-50">
                            {item.q}
                          </span>
                          <ChevronDown
                            className={`mt-1 h-4 w-4 text-slate-400 chevron-rotate ${
                              isOpen ? "rotate-180 text-emerald-300" : ""
                            }`}
                          />
                        </button>
                        <div
                          className="faq-body overflow-hidden"
                          style={{
                            maxHeight: isOpen ? "200px" : "0px",
                            opacity: isOpen ? 1 : 0,
                            transform: isOpen ? "translateY(0)" : "translateY(-6px)",
                          }}
                        >
                          <p className="mt-2.5 text-xs text-slate-300">
                            {item.a}
                          </p>
                        </div>
                      </div>
                    );
                  })}
                </div>
              </div>
            </div>
          </section>

          {/* CTA BANNER */}
          <section
            id="cta"
            ref={ctaRef}
            className="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8 py-10 sm:py-14 lg:py-16"
          >
            <div
              className={`section-hidden ${ctaVisible ? "section-visible" : ""}`}
            >
              <div className="relative cta-gradient rounded-3xl p-5 sm:p-7 lg:p-9 overflow-hidden">
                {/* decorative orbs */}
                <div className="pointer-events-none absolute -right-10 -bottom-10 h-44 w-44 rounded-full bg-cyan-500/30 blur-3xl" />
                <div className="pointer-events-none absolute -left-10 -top-12 h-36 w-36 rounded-full bg-emerald-500/40 blur-3xl" />

                <div className="relative flex flex-col lg:flex-row items-center justify-between gap-6">
                  <div className="max-w-xl space-y-3">
                    <p className="text-xs uppercase tracking-[0.25em] text-emerald-100/90">
                      Готовы к следующему шагу
                    </p>
                    <h2 className="text-2xl sm:text-3xl font-semibold tracking-tight text-slate-50">
                      Получите персональный ипотечный план за 24 часа
                    </h2>
                    <p className="text-sm text-slate-100/80">
                      Оставьте контакт — брокер свяжется с вами, задаст несколько
                      уточняющих вопросов и пришлёт расчёт по 2–3 лучшим схемам
                      под ваш запрос.
                    </p>
                    <p className="text-[11px] text-emerald-100/80">
                      Без спама и обязательств. Если мы не сможем улучшить ваши
                      условия — честно скажем об этом.
                    </p>
                  </div>

                  <div className="w-full max-w-md space-y-3">
                    <div className="flex flex-col sm:flex-row gap-3">
                      <div className="flex-1">
                        <label className="sr-only">Email</label>
                        <div className="input-glass rounded-full px-4 py-2.5 flex items-center gap-2">
                          <Mail className="h-4 w-4 text-slate-400" />
                          <input
                            type="email"
                            placeholder="Ваш email"
                            className="w-full bg-transparent text-xs text-slate-100 placeholder:text-slate-500 focus:outline-none"
                          />
                        </div>
                      </div>
                      <div className="flex-1">
                        <label className="sr-only">Телефон</label>
                        <div className="input-glass rounded-full px-4 py-2.5 flex items-center gap-2">
                          <Phone className="h-4 w-4 text-slate-400" />
                          <input
                            type="tel"
                            placeholder="+7 (___) ___-__-__"
                            className="w-full bg-transparent text-xs text-slate-100 placeholder:text-slate-500 focus:outline-none"
                          />
                        </div>
                      </div>
                    </div>
                    <button className="btn-primary w-full rounded-full px-4 py-2.5 text-xs font-semibold flex items-center justify-center gap-2 min-h-[44px]">
                      <span>Получить расчёт и план действий</span>
                      <ArrowRight className="h-4 w-4" />
                    </button>
                    <p className="text-[10px] text-slate-200/80">
                      Нажимая на кнопку, вы соглашаетесь с обработкой персональных
                      данных и политикой конфиденциальности. Мы не передаём ваши
                      данные третьим лицам без необходимости для сделки.
                    </p>
                  </div>
                </div>
              </div>
            </div>
          </section>
        </main>

        {/* FOOTER */}
        <footer className="border-t border-slate-800/80 bg-slate-950/90 backdrop-blur-xl">
          <div className="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8 py-8 sm:py-10">
            <div className="grid gap-8 sm:grid-cols-5">
              <div className="sm:col-span-2 space-y-3">
                <div className="flex items-center gap-2">
                  <div className="flex h-9 w-9 items-center justify-center rounded-2xl bg-slate-900 border border-emerald-400/70 shadow-lg shadow-emerald-500/50">
                    <Building2 className="h-5 w-5 text-emerald-400" />
                  </div>
                  <div className="flex flex-col leading-tight">
                    <span className="text-sm font-semibold tracking-tight text-slate-50">
                      Emerald
                      <span className="text-emerald-400">Mortgage</span>
                    </span>
                    <span className="text-[11px] text-slate-400">
                      Ипотечный брокер / финансовая архитектура
                    </span>
                  </div>
                </div>
                <p className="text-xs text-slate-400 max-w-sm">
                  Помогаем выстраивать ипотеку как инструмент, а не как долговую
                  яму: с учётом ваших целей, рисков и будущих шагов.
                </p>
                <div className="flex items-center gap-2">
                  <span className="inline-flex h-1.5 w-1.5 rounded-full bg-emerald-400 animate-pulse" />
                  <span className="text-[11px] text-slate-400">
                    Принимаем ограниченное число клиентов в месяц
                  </span>
                </div>
              </div>

              <FooterColumn
                title="Навигация"
                links={[
                  "Преимущества",
                  "Как это работает",
                  "Тарифы",
                  "Отзывы",
                  "FAQ",
                ]}
              />
              <FooterColumn
                title="Сервис"
                links={[
                  "Новостройки",
                  "Вторичный рынок",
                  "Рефинансирование",
                  "Инвест‑ипотека",
                ]}
              />
              <FooterColumn
                title="Ресурсы"
                links={[
                  "Блог",
                  "Гид по ипотеке",
                  "Чек‑листы",
                  "Политика конфиденциальности",
                ]}
              />
              <FooterColumn
                title="Контакты"
                links={["+7 (999) 000‑00‑00", "hello@emerald.mortgage", "Telegram‑канал", "Карьера"]}
              />
            </div>

            <div className="mt-8 flex flex-col sm:flex-row items-center justify-between gap-4 border-t border-slate-800/70 pt-5">
              <p className="text-[11px] text-slate-500">
                © {new Date().getFullYear()} EmeraldMortgage. Все права защищены.
                Не является публичной офертой.
              </p>
              <div className="flex items-center gap-2">
                <a
                  href="#"
                  className="social-icon flex h-8 w-8 items-center justify-center rounded-full border border-slate-700/70 text-slate-400"
                >
                  <Github className="h-4 w-4" />
                </a>
                <a
                  href="#"
                  className="social-icon flex h-8 w-8 items-center justify-center rounded-full border border-slate-700/70 text-slate-400"
                >
                  <Twitter className="h-4 w-4" />
                </a>
                <a
                  href="#"
                  className="social-icon flex h-8 w-8 items-center justify-center rounded-full border border-slate-700/70 text-slate-400"
                >
                  <Linkedin className="h-4 w-4" />
                </a>
                <a
                  href="#"
                  className="social-icon flex h-8 w-8 items-center justify-center rounded-full border border-slate-700/70 text-slate-400"
                >
                  <Instagram className="h-4 w-4" />
                </a>
              </div>
            </div>
          </div>
        </footer>
      </div>
    </>
  );

  // Small inner components
  function HeroMetric({ label, value, accent, icon }) {
    return (
      <div className="rounded-2xl bg-slate-900/80 border border-slate-700/60 p-3 flex flex-col gap-1">
        <div className="flex items-center justify-between gap-2 text-[11px] text-slate-400">
          <span>{label}</span>
          {icon}
        </div>
        <div
          className={`text-sm font-semibold ${
            accent ? "gradient-text" : "text-slate-100"
          }`}
        >
          {value}
        </div>
      </div>
    );
  }

  function ProgramRow({ bank, rate, note }) {
    return (
      <div className="flex items-center justify-between gap-2 rounded-xl bg-slate-900/80 border border-slate-700/70 px-3 py-2">
        <div className="text-[11px]">
          <p className="font-medium text-slate-100">{bank}</p>
          <p className="text-slate-400">{note}</p>
        </div>
        <div className="text-sm font-semibold text-emerald-300">{rate}</div>
      </div>
    );
  }

  function StatBlock({ label, value, suffix, start }) {
    return (
      <div className="rounded-2xl bg-slate-950/80 border border-slate-800/80 px-4 py-3 flex items-center justify-between gap-4">
        <div className="text-xs text-slate-300 max-w-[60%]">{label}</div>
        <div className="text-right">
          <div className="text-2xl font-semibold gradient-text">
            <StatCounter value={value} suffix={suffix} start={start} />
          </div>
        </div>
      </div>
    );
  }

  function FooterColumn({ title, links }) {
    return (
      <div className="space-y-3 text-xs">
        <h4 className="font-semibold text-slate-100">{title}</h4>
        <ul className="space-y-1">
          {links.map((l) => (
            <li key={l} className="text-slate-400 hover:text-slate-100 cursor-pointer">
              {l}
            </li>
          ))}
        </ul>
      </div>
    );
  }

  function SparklesSmall(props) {
    return (
      <svg
        viewBox="0 0 24 24"
        className="h-3.5 w-3.5 text-emerald-300"
        aria-hidden="true"
        {...props}
      >
        <path
          fill="currentColor"
          d="M12 2.5 13.2 7l4.3 1.2L13.2 9.5 12 14l-1.2-4.5-4.3-1.3L10.8 7 12 2.5Zm6.2 6.3.6 2.2 2.2.6-2.2.6-.6 2.2-.6-2.2-2.2-.6 2.2-.6.6-2.2Zm-12 2.2.8 3 3 .8-3 .8-.8 3-.8-3-3-.8 3-.8.8-3Z"
        />
      </svg>
    );
  }
};

export default MortgageLandingPage;